---
type: llm
---
PASS if the answer raises a concern connected to this fulfillment rule: once an order's status becomes "Dispatched," its delivery address is locked and can no longer be changed through the app (the customer would need to contact support, who can only redirect if the courier hasn't yet attempted delivery) — for example, flagging that the address-change screen and its "Address updated" confirmation don't indicate which orders the change actually applies to, risking a customer believing a dispatched order's address changed when it did not. FAIL if the answer never mentions this dispatched-order address-lock limitation at all.
